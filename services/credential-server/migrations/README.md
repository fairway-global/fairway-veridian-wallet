These SQL files define the relational schema for dashboard template and credential management.

Current runtime storage in this repository is JSON-backed (`data/dashboard-db.json`) for lightweight local deployment, while these migrations provide the production DB shape.
