name: deploy-prod

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      release_sha:
        description: Release SHA/image tag to deploy
        required: false
        default: ""
      target_slot:
        description: blue, green, or auto
        required: false
        default: auto
      action:
        description: promote or rollback
        required: false
        default: promote

jobs:
  deploy:
    uses: ./.github/workflows/deploy-reusable.yaml
    with:
      environment: production
      release_sha: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_sha != '' && github.event.inputs.release_sha || github.sha }}
      target_slot: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.target_slot != '' && github.event.inputs.target_slot || 'auto' }}
      action: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.action != '' && github.event.inputs.action || 'promote' }}
      check_public: true
    secrets:
      VM_HOST: ${{ secrets.PROD_VM_HOST }}
      VM_USER: ${{ secrets.PROD_VM_USER }}
      VM_SSH_KEY: ${{ secrets.PROD_VM_SSH_KEY }}
      GHCR_USERNAME: ${{ secrets.GHCR_USERNAME }}
      GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
      PUBLIC_DOMAIN: ${{ secrets.PROD_PUBLIC_DOMAIN }}
      KERIA_PASSCODE: ${{ secrets.PROD_KERIA_PASSCODE }}
      DATABASE_URL: ${{ secrets.PROD_DATABASE_URL }}
      JWT_ACCESS_SECRET: ${{ secrets.PROD_JWT_ACCESS_SECRET }}
      JWT_REFRESH_SECRET: ${{ secrets.PROD_JWT_REFRESH_SECRET }}
      GATEWAY_JWT_SECRET: ${{ secrets.PROD_GATEWAY_JWT_SECRET }}
      BRAN_ENCRYPTION_KEY: ${{ secrets.PROD_BRAN_ENCRYPTION_KEY }}
      GCP_PROJECT_ID: ${{ secrets.PROD_GCP_PROJECT_ID }}
